# DOC-RECONCILIATION.md

This file was generated by a `/doc-truth-up` documentation-reconciliation pass. That pass treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state — it changed no source code, configs, or tests, and ran no builds or test suites. Evidence for
each claim was gathered by reading source files, manifests, and git history. This file explains what
was found and what was fixed, so any reviewer can audit or revert individual changes.

---

## Per-Claim Findings

### 1. What It Is
**Status: consistent**
**Evidence: verified-by-reading-code**

`backend/main.py`, `backend/routes/`, `backend/scanner/`, `backend/cve/`, `frontend/src/` — all
match the README and CLAUDE.md descriptions. ARP sweep (scapy), nmap enrichment, device
classification, risk engine, CVE matching, Cytoscape.js topology graph, SQLite history, and
APScheduler scheduling are all present as functional, non-stub code. No changes made.

---

### 2. Current State
**Status: consistent**
**Evidence: verified-by-reading-code**

`CLAUDE.md` (portfolio-context) correctly states `**Phase 4: Complete (Phases 0–4 shipped)**`.
All implementation files for all four phases are present (`backend/scanner/`, `backend/cve/`,
`backend/routes/`, `backend/db/`, all frontend components). No changes made.

---

### 3. Stack
**Status: consistent**
**Evidence: verified-by-reading-code**

`frontend/package.json` and `backend/requirements.txt` match `CLAUDE.md` and `README.md` stack
claims at every version constraint:

| Item | Doc claims | Actual |
|------|-----------|--------|
| python-nmap | `≥0.7` | `>=0.7,<1` (`backend/requirements.txt:3`) |
| React | `19+` | `^19.2.4` (`frontend/package.json:17`) |
| Vite | `8+` | `^8.0.1` (`frontend/package.json:34`) |
| Cytoscape.js | `3.33+` | `3.33.1` (`frontend/package.json:14`) |
| TypeScript | `5+` | `~5.9.3` (`frontend/package.json:32`) |
| Tailwind CSS | `4` | `4.2.2` (`frontend/package.json:22`) |

No changes made.

---

### 4. How to Run
**Status: drifted → fixed**
**Evidence: verified-by-reading-code**

Three stale path references found in `CLAUDE.md`. The actual test entry point is
`backend/tests/` (confirmed by `ci.yml:20`: `pytest backend/tests -v`). There is no `src/`
directory at the repo root (confirmed by directory listing) and no root-level `tests/`
directory — all Python source lives under `backend/`.

**Changed (`CLAUDE.md`, `## Build / Test / Run` block):**
- `python -m pytest tests/ -v` → `pytest backend/tests/ -v`
  (`tests/` does not exist; CI uses `pytest backend/tests`)
- `ruff check src/ tests/` → `ruff check backend/`
  (`src/` does not exist; source is in `backend/`)

**Changed (`CLAUDE.md`, `## Conventions` section):**
- `` `src/scanner/` `` → `` `backend/scanner/` ``
  (actual scanner module path; `src/` directory does not exist at repo root)

---

### 5. Known Risks
**Status: consistent**
**Evidence: verified-by-reading-code**

`CLAUDE.md` conventions and portfolio-context "Known Risks" are all code-enforced:
- Whitelist validation before scan: `backend/routes/scans.py` validates against whitelist before
  invoking orchestrator.
- No direct subprocess from routes: all scan calls route through `backend/scanner/orchestrator.py`.
- No sync CVE load at startup: `backend/main.py:51–63` — CVE index loaded only if
  `nvd_download_complete` flag is set; download is background-only.
- Hooks-only React: `frontend/src/` contains only function components.

No changes made.

---

### 6. Next Move
**Status: consistent**
**Evidence: verified-by-reading-code**

Phase 4+ deferred items (PDF export, push notifications, custom classification rules UI) are
correctly absent from the code. No changes made.

---

## Changes to `docs/PORTFOLIO-DISPOSITION.md`
**Status: drifted → fixed**
**Evidence: verified-by-reading-code**

`docs/` is in the editable scope. Three factual claims were wrong:

**Run command (two occurrences):**
- `docs/PORTFOLIO-DISPOSITION.md:131`: `sudo python main.py` → `./scripts/run.sh`
  (`backend/main.py` has no `__main__` block; `scripts/run.sh:22` is the actual entry point)
- `docs/PORTFOLIO-DISPOSITION.md:199`: `sudo python main.py` → `./scripts/run.sh`

**Test count (two occurrences):**
- `docs/PORTFOLIO-DISPOSITION.md:186`: `~35 tests` → `~150 tests (code-verified)`
  (Grep of `def test_` + `async def test_` across `backend/tests/*.py` yields ~150 test
  functions across 18 test files — not ~35)
- `docs/PORTFOLIO-DISPOSITION.md:201`: `~35 per memory` → `~150 (code-verified)`

**Reactivation procedure path:**
- `docs/PORTFOLIO-DISPOSITION.md:186`: `pytest backend/` → `pytest backend/tests/`
  (correct invocation path per `ci.yml:20`)

---

## Contradictions for Manual Review

Files outside the editable set that have doc↔code drift. A human should apply these corrections:

### `IMPLEMENTATION-ROADMAP.md` — stale install commands (carried from prior pass, still unresolved)
- **`IMPLEMENTATION-ROADMAP.md:498`**: `npm install react@18 react-dom@18`
  → should be `react@19 react-dom@19` (actual: `^19.2.4` in `frontend/package.json`)
- **`IMPLEMENTATION-ROADMAP.md:500`**: `vite@5`
  → should be `vite@8` (actual: `^8.0.1`)
- **`IMPLEMENTATION-ROADMAP.md:500`**: `tailwindcss@3`
  → should be `tailwindcss@4` (actual: `4.2.2`); also `@tailwindcss/vite` plugin is required
  (`frontend/vite.config.ts:3`) but not listed in the install command.
- **`IMPLEMENTATION-ROADMAP.md:497`**: `requests==2.32.*`
  → should be `requests>=2.33,<3` (actual: `backend/requirements.txt:7`, bumped in `6bebd57`)

### `CHANGELOG.md` — reflects no shipped work
- **`CHANGELOG.md:7–9`**: Only entry is `Initial release` under `[Unreleased]`. The project has
  completed all four implementation phases plus security hardening. A human should fill in at
  least the v1.0 entry.

### `Makefile` — stale paths (introduced since prior pass)
- **`Makefile:4`**: `pip install -r requirements.txt`
  → should be `pip install -r backend/requirements.txt` (no `requirements.txt` at repo root)
- **`Makefile:7`**: `python -m pytest tests/ -v`
  → should be `pytest backend/tests/ -v` (no root-level `tests/` directory)
- **`Makefile:10`**: `ruff check src/ tests/`
  → should be `ruff check backend/` (no `src/` directory)
- **`Makefile:13`**: `python -m src`
  → invalid (`src` is not a module); correct entry point is `./scripts/run.sh`

### `docs/PORTFOLIO-DISPOSITION.md` — stale origin/main tip SHA
- **`docs/PORTFOLIO-DISPOSITION.md:27–28`**: Lists `6bebd57` as the `origin/main` tip. The
  current HEAD is `2d67b98` (docs-only commits since `6bebd57`). The code-level analysis in
  the file remains accurate; only the SHA reference is stale. A human may update if desired.

---

## Footer

**Generated:** 2026-06-02 19:35:32 PDT
**Branch:** docs/truth-up-2026-06-02
**HEAD reconciled against:** 2d67b982c5fe058b4f30c370144ecae6edbc70f3
**Doc files changed:** `CLAUDE.md`, `docs/PORTFOLIO-DISPOSITION.md`, `DOC-RECONCILIATION.md` (overwritten)
